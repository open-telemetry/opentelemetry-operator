# One of 'breaking', 'deprecation', 'new_component', 'enhancement', 'bug_fix'
change_type: enhancement

# The name of the component, or a single word describing the area of concern, (e.g. collector, target allocator, auto-instrumentation, opamp, github action)
component: target allocator

# A brief description of the change. Surround your text with quotes ("") if it needs to start with a backtick (`).
note: "Add a `consistent-hashing-endpoint` allocation strategy that keys the hash on the target's scrape endpoint instead of on `__address__` alone."

# One or more tracking issues related to the change
issues: [5321]

# (Optional) One or more lines of additional information to render under the primary note.
# These lines will be padded with 2 spaces and then inserted directly into the document.
# Use pipe (|) for multiline entries.
subtext: |
  `consistent-hashing` hashes only `__address__`, so scrape endpoints that share a host:port
  but are distinguished by metrics path or query params (for example, multiple exporters behind
  the same proxy) always land on the same collector. `consistent-hashing-endpoint` hashes the
  address, scheme, metrics path, and query params together, spreading those endpoints across
  collectors while staying insensitive to labels that don't affect the scrape URL.
