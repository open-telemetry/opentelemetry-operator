name: "End-to-end tests"

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

permissions:
  contents: read

jobs:
  run-e2e-tests:
    uses: ./.github/workflows/e2e-reusable.yaml
