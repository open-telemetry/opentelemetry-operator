apiVersion: splunk.com/v1alpha1
kind: SplunkOtelAgent
metadata:
  name: splunkotelagent-sample
spec:
  config: |
    receivers:
      jaeger:
        protocols: 
          grpc:

    exporters:
      logging:

    service:
      pipelines:
        traces:
          receivers: [jaeger]
          exporters: [logging]
