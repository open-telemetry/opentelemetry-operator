metadata:
  name: test
  namespace: test
spec:
  image: test
  replicas: 1
  serviceAccount: my-special-sa
  mode: deployment
  config:
    receivers:
      examplereceiver:
        endpoint: "0.0.0.0:12345"
    exporters:
      debug:
    service:
      pipelines:
        metrics:
          receivers: [examplereceiver]
          exporters: [debug]
