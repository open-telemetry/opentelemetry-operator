extensions:
  k8s_leader_elector: {}
receivers:
  otlp:
    protocols:
      grpc:
        endpoint: 0.0.0.0:4317
processors: {}
exporters:
  debug:
    verbosity: basic
service:
  extensions: [k8s_leader_elector]
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [debug]
