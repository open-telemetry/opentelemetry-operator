apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-deno
spec:
  selector:
    matchLabels:
      app: my-deno
  replicas: 1
  template:
    metadata:
      labels:
        app: my-deno
      annotations:
        sidecar.opentelemetry.io/inject: "true"
        instrumentation.opentelemetry.io/inject-deno: "true"
    spec:
      securityContext:
        runAsUser: 1000
        runAsGroup: 3000
        fsGroup: 3000
      containers:
      - name: myapp
        image: ghcr.io/open-telemetry/opentelemetry-operator/e2e-test-app-deno:main
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop: ["ALL"]
