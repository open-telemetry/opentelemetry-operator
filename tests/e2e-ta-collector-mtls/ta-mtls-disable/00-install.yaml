apiVersion: opentelemetry.io/v1alpha1
kind: TargetAllocator
metadata:
  name: mtls-disable
spec:
  serviceAccount: ta
  mtls:
    enabled: true
  scrapeConfigs:
    - job_name: prometheus
      static_configs:
        - targets: ["localhost:9090"]
