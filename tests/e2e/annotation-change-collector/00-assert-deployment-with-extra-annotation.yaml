apiVersion: apps/v1
kind: Deployment
metadata:
  name: deployment-collector
  annotations:
    user-annotation: "existing"
spec:
  template:
    metadata:
      annotations:
        user-annotation: "existing"
