apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: statefulset-collector
  annotations:
    user-annotation: "existing"
spec:
  template:
    metadata:
      annotations:
        user-annotation: "existing"
